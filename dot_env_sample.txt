# Which environment is this deployment running
# dev | test | stage | prod
ENV=dev

DETAILED_LOGGING=false
LOG_MODEL_USAGE=true
PRINT_TRACEBACK=true

# Where are model files, metadata etc. saved
# local_file | local_s3 | s3
# Change these according to value of ENV

STORAGE_TYPE=local_s3
LOCAL_DIR=models

S3_BUCKET_NAME=my-bucket
STORAGE_LOCATION=local

DATABASE_TYPE=dynamodb
DATABASE_LOCATION=local
METADATA_TABLE=MyTable
TABLE_PREFIX=Dev

# Email configuration
SEND_EMAIL=yes
EMAIL_SERVER=smtp.mailgun.org
EMAIL_PORT=587
EMAIL_USER=<email of user who can send authorized emails for this app>
EMAIL_PASSWORD=<password for the above email user>
EMAIL_VERIFICATION_URL=http://localhost:5000/verify_email

PASSWORD_RESET_URL=http://localhost:5000/reset_password
PASSWORD_RESET_TOKEN_VALIDITY_SECONDS=3600
LOGIN_VALIDITY_SECONDS=3600
JWT_SECRET=<a long random string>
